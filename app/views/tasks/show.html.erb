

<h2><%= @task.details %> </h2>
<div class="text-decoration-none d-flex justify-content-evenly">
<%= link_to "Back to tasks", tasks_path %>

<%= link_to "Edit this item", edit_path %>

<%= link_to "🗑",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
</div>
